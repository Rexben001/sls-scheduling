{
  "name": "aws-node-scheduled-cron-project",
  "version": "1.0.0",
  "description": "Example of creating a function that runs as a cron job using the serverless `schedule` event",
  "author": "",
  "license": "MIT",
  "scripts": {
    "local": "serverless offline",
    "watch": "tsc -w",
    "start":"tsc && serverless invoke local --function rateHandler",
    "cron":"serverless offline start"
  },
  "devDependencies": {
    "@serverless/typescript": "^3.21.0",
    "@types/aws-lambda": "^8.10.106",
    "serverless-offline": "^5.12.1",
    "serverless-offline-scheduler": "^0.5.0",
    "serverless-plugin-typescript": "^1.1.9",
    "ts-node": "^8.9.1",
    "typescript": "^3.8.3"
  },
  "dependencies": {
    "aws-lambda": "^1.0.7",
    "serverless-mysql": "^1.5.4"
  }
}
